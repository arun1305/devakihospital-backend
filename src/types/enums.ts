export enum UserRole {
  ADMIN = "admin",
  EDITOR = "editor",
  DOCTOR = "doctor",
  RECEPTIONIST = "receptionist",
}

export enum AppointmentStatus {
  PENDING = "pending",
  APPROVED = "approved",
  REJECTED = "rejected",
  COMPLETED = "completed",
  CANCELLED = "cancelled",
}

export enum ContentStatus {
  DRAFT = "draft",
  PUBLISHED = "published",
  ARCHIVED = "archived",
}

export enum Gender {
  MALE = "male",
  FEMALE = "female",
  OTHER = "other",
}
